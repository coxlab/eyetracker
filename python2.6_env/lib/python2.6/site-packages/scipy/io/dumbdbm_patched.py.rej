***************
*** 70,76 ****
          f.close() 
      def __getitem__(self, key): 
-         pos, siz = self._index[key] # may raise KeyError 
          f = _open(self._datfile, 'rb') 
          f.seek(pos) 
          dat = f.read(siz)
  
--- 70,79 ----
          f.close() 
      def __getitem__(self, key): 
+         try: 
+             pos, siz = self._index[key] # may raise KeyError 
+         except KeyError: 
+             raise IndexError 
          f = _open(self._datfile, 'rb') 
          f.seek(pos) 
          dat = f.read(siz)
  
